{
  "version": "0.2.0",
  "configurations": [{
    "name": "lldblaunch",
    "type": "lldb",
    "request":"launch",
    "stopOnEntry": false,
    "preLaunchTask": "build",
    "cwd":"${workspaceFolder}",
   "program": "${workspaceFolder}/main"
    
  }
  ]
}

  